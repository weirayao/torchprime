# The default config file. You may override configs with `key=value` arguments on the CLI
# according to https://hydra.cc/docs/advanced/override_grammar/basic/.

# This defines the order in which configs are loaded. The latter configs
# override the earlier ones.
defaults:
  - _self_ # refers to this config file
  - model: llama-3-8b # refers to model/llama-3-8b.yaml
  - data: wikitext

global_batch_size: 4
logging_steps: 10
max_steps: 45000

run_name: null

reshape_context: false
seg_attn: false

# Training mode: "pretrain" or "midtrain" or "sft"
training_mode: pretrain

# SFT related
progress_src_mask: false
progress_src_mask_ratio: 0

seed: 42
profile_step: -1

# This might be overwritten when using tp run to launch the run using XPK
profile_dir: profile
profile_duration: 100000

# This might be overwritten when using tp run to launch the run using XPK
output_dir: outputs

# Checkpoint directory
checkpoint_load_dir: checkpoints/ # for loading checkpoints
checkpoint_load_step: null # checkpoint step to load, can be "latest" or a specific saved step
resume_from_checkpoint: false # whether to resume from checkpoint and load the optimizer, scheduler, and masking scheduler
checkpoint_save_dir: null # for saving checkpoints
save_steps: 1000
# DO NOT set in recipe they are for resuming from checkpoint only and set by the trainer:
steps_to_skip: 0 # Number of steps to skip when resuming from checkpoint
all_data_files: null
dataset_name: null
is_resuming_epoch: false

checkpoint_dir_for_midtrain: checkpoints/
resume_for_midtrain: null
continue_for_masked_midtrain: null

optimizer:
  learning_rate: 2.e-4
lr_scheduler:
  type: linear
  warmup_steps: 150

# The virtual device mesh shape to use within a TPU slice. This is also called
# the "ICI mesh", since devices within a slice enjoy a faster network called
# "Inter-Chip Interconnect".
ici_mesh:
  data: 1
  fsdp: 4
  tensor: 1
  expert: 1

# Shape of the logical mesh where each element is a TPU slice. This is called
# "Data Center Network (DCN) mesh" because TPU slices are usually connected
# together with slower data center networking, with the faster ICI network
# used within a slice.
#
# As an example, to enable 2-way data parallelism across 2 TPU slices, you may
# specify `dcn_mesh.data=2`.
dcn_mesh:
  data: 1
  fsdp: 1
  tensor: 1
  expert: 1

# These are default values for model activation rematerialization configuration.
# They can be overridden on the command line or by importing one of the presets
# in the `model/remat` directory.
model:
  # prefix masking probability - with this probability, randomly sample a prefix length that won't be maskable
  prefix_probability: 0
  # truncate probability - with this probability, truncate non-prefixed rows at random position and fill with pad token
  truncate_probability: 0
  # block masking probability - with this probability, randomly sample a block size and mask with it
  block_masking_probability: 0
  
  # Masking scheduler configuration
  masking_scheduler:
    # Schedule type: "constant" or "linear"
    schedule_type: "constant"
    # For linear schedule: number of steps to reach target probabilities
    max_schedule_steps: null
  # mask block sizes - with this probability, randomly sample a block size and mask with it
  mask_block_sizes: [2, 4, 8]

  remat:
    # The class names of model layers whose intermediate activations should be
    # recomputed during the backward pass (i.e. activation checkpointing).
    activation_checkpoint_layers: []

    # If not null, compile a module of type `HomogeneousSequential` located at the
    # given path in the module tree using `torch_xla.experimental.scan_layers`.
    scan_layers: null

    # If specified, offload these tensors to host RAM during the forward pass and
    # move them back during the backward pass.
    #
    # The tensors to be offloaded should be given a name by wrapping them with the
    # `torchprime.torch_xla_models.offloading.offload_name` call. Then the same
    # name could be specified here to offload that tensor.
    #
    # Currently in order to offload tensors, `scan_layers` must also be enabled.
    offload_tensors: []
